var users = ["nashra", "ruby", "laiba"];
if (users.length == 0) {
    console.log("we need to find some users");
}
else {
    console.log("greet user");
}
users = [];
if (users.length === 0) {
    console.log("We need to find some users!");
}
